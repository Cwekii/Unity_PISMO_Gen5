DOMINIK CVETKOVSKI
095 308 9202
Napisati poruku na whatsapp:
Ime i prezime

Za HZZ - TINA NOVAK tnovak@rk-smz.hr
O edukaciji razno - TIJANA ŠAPINA tsapina@simora.hr


Matematički zapis:
5 + 8 = 13

Programski zapis:
13= 5 + 8

1.) 
x + 8 = 13
x = 13 - 8

x = 5
13 = 8 + x

- VRIJABLA - mjesto u memoriji rezervirano za pohranu podataka

- INT (int) - za cijele brojeve (3, 20, -2, 975211321, 0)

int X = 3
int Y = 2
int Z = 0

Koliki je Z ako se u njega sprema zbroj X i Y?

Z = X + Y

Napišite 3 varijable vaših naziva koje su sve sa cijelim brojevima. Sve 3 varijable se zbaraju i zapisuje se rezultat u varijablu "razultat"

int vino = 3
int voda = 1
int gemišt = 4
int rezultat = 0

rezultat = vino + voda + gemišt

Float - za decimalne brojeve (5.3, 6.0, 4.5, 0.2, -3.2, 0.0)

float mama = 5.6f
float tata = 25.0f (25f)
float beba = 0f

beba = mama + tata

4.) Napišite 3 varijable float vrijednosti gdje se od prve oduzima zbroj druge i treće. Rezultat matematičke jednadžbe se zapisuje u prvu varijablu.

float rijeka = 5f
float kaštela = 6.2f
float brač = 1.2f

rijeka = rijeka - (kaštela + brač)

5.) Koliki je zbroj X i Y ako znamo da je:
float X = 4.55f
int Y = 69
float XY = 0

XY = X + Y

6.) Zapištie programski sljedeći zapis:

weed + mars - uho = droga
uho * 2.5 + pussy = slon
slon * droga = rezultat

int weed = 3
int mars = 8
int uho = 4
int droga = 0

int pussy = 1
float slon = 0f
float rezultat = 0f

droga = weed + mars - uho (7)
slon = uho * 2.5f + pussy (11)
rezultat = slon * droga (77)

7.) Zbroji x i y ako je x 5 , a y je 8.

int x = 5
int y = 8

x = x + y

8.) kolika je razlika između x i y ako je x 19, a y je 7.5? Rezultat zapišite u X

int x = 19
float y = 7.5f
float X = 0

X = x - y


9.)Programski zapišite ovaj matematički zapis: 9 + 11 = 20 (Obavezno definirajte i imenujte varijable)

int x = 9
int y = 11
int z = 0

z = x + y

10.) Početna vrijednost healtha je 100, primamo damage u vrijednosti 17.6, kolika je konačna vrijednost healtha?

float health = 100f
float damage = 17.6f

health = health - damage
*NAPOMENA*
health -= damage

11.) Pretvorite u programski zapis sa vašim vrijednostima:
susjed + mama - tata * krevet = problem

int susjed = 25
int mama = 35
int tata = 40
int krevet = 2
int problem = 0

problem = susjed + mama - tata * krevet

12.) Pretvorite u programski zapis sa vašim vrijenostima:
bomba * 2 + teroristi - majmun / 3.2 = wtf

int bomba = 1
int teroristi = 100
int majmun = 5
float wtf = 0

wtf = bomba * 2 + teroristi - majmun / 3.2f 


13.) Na početku vaš lik ima 52 mane, iskoristili ste ability koji košta 4.5 mana i potom ste ga iskoristili još 3 puta. Poslje toga popijtete potion koji vam da 10 mana.
Koliko imate mane? Napišite programski.

float mama = 52f
float ability = 4.5f
int potion = 10

mana = mana - ability
mana = mana - (ability * 3)
mana = mana + potion ILI mana += potion

* Točka zarez " ; " se piše na kraju linije naredbe

npr.

float mana = 56.4f;
int konj = 3;
float rez = 0;

rez = mama * konj;

Što je to "Null" a što "0" - sjetite se prikaza WC papira

int rollaPapira = 6;
maknemo papire sa role i onda bude
int rollaPapira = 0;
maknemo i rolu
int rollaPapira; (Njegova vrijednost je null odnosno brojčano 0)

rollaPapira = rollaPapira * 100;


14.) Ušli ste u borbu. U šanžeru imate 15 metaka od maksimalnih 20. Ispucali ste ih 4, te potom reloadali šanžer. Napište to programski.

int šanžerCurrent = 15;
int šanžerMax = 20;

šanžerCurrent = šanžerCurrent - 4;
šanžerCurrent = šanžerMax;

15.) U varijablu "students" spremite količinu učenika u učioni, prvo ih dođe 20, nakon toga ih ode 5, zatim dođe njih 4, te potom ih ode 12 i za kraj se broj učenika udvostručio. 
Napišite to programski, ali obavezno korak po korak događaja.

int s = 0; ili int s;

s += 20;
s -= 5;
s += 4;
s -= 12;
s *= 2;


16.) Na početku prvi igrač ima 12 ponija, drugi igrač ima 10 ponija. Koliko ponija ima koji igrač ako prvi otme drugome 4 ponija?
Napišite programski.

int playerOne = 12;
int playerTwo = 10;

playerOne += 4;
playerTwo -= 4;


17.) Zbrojite x koji je 17.2 i y koji je 12.32. Rezultat napišite u varijabli "Maska"

float x = 17.2f; 
float y = 12.32f;
float Maska = x + y;

18.) Povećajte vrijednost x za vrijednost x ako je x 5

int x = 5;

x += x;

19.) Neka je x vrijednosti 6, a y je 7. Njihov rezultat zbrajanja uvećajte (množenje) za vrijednost y i spremite u z

int x = 6;
int y = 7;
int z = 0;

z = (x + y) * y;

20.) Igrač ima 100 HP-a, pao je sa visine 8 metara i zgubio 20% HP-a. Zatim mu se HP regenerirao za 8 (brojčano) i potom je pokušao ponovno skočiti, ali je sada pao sa visine 7 metara i izgubio 15.7% HP-a.
Napišite ovo programski. (Postoci se odnose na trenutni, a ne na maksimalni iznos)

float igracHP = 100f;

igracHP -= 0.2f * igracHP;
igracHP += 8;
igracHP -= 0.157 * igracHP;


21.) Na početku je z 12, i prvo ga povećamo za 3, a zatim ga umanjimo za 2%.
Napišite ovo programski.

int z = 12;

z += 3;
z *= 0.98f;

22.) Imate 10 pokrajina. Svaka pokrajina po potezu daje 1 zlatnik. Nakon 3 poteza si izgubio 50% pokrajina. Prošlo je još 2 poteza. Koliko imaš zlatnika na kraju? 
Napišite programski.

float pokrajina = 10f;
int zlatnik = 0;

zlatnik = pokrajina * 3 * 1; (nakon 3 poteza imamo 30 zlatnika)
pokrajina *= 0.5f; (sada imamo 5 pokrajina)
zlatnik += pokrajina * 2 * 1; (nakon još 2 poteza imamo 40)

23.) Na početku je bilo 100 navijača, od njih ih je 12 donjelo baklje (njih nisu pustili). Redari nisu pustili na utakmicu svakog drugog navijača. Koliko je navijača na kraju.

int navijači = 100;
int hooligani = 12;

navijači -= hooliani;
navijači *= 0.5f;

24.) Igrač ima 250 kristala, dao je 80 kristala čarobnjaku. Potom je prošao level i zaradio 70 kristala. Koliko igrač ima kristala, a koliko kristala ima čarobnjak?

int kIgrača = 250;
int kČarobnjak = 0;

kIgrač -= 80;
kČarobnjak += 80;
kIgrač += 70;

kIgrač = 240;
kČarobnjak = 80;

25.) Baka je dedi dala 12 kuna (za pivu), a deda je već imao 12 kuna, došla je vrana i uzela baki 8 jabuka, a baka je imala ukupno 10 jabuka. Koliko ce deda popiti piva ako je jedna piva 6 kn, a koliko jabuka ima baka?

int kuneDeda = 12;
int jabuke = 10;
int cijenaPive = 6;
int popijenoPive = 0;

kuneDeda += 12;
jabuke -= 8;
popijenoPive = kuneDeda / cijenaPive;

deda je popio 4 pive, a baka ima 2 jabuke.

26.) Max inventory je 100, trenutno je pun 67, igrač izabi 13 nepotrebnih itema. Potom ubije trolla i uzme 45 itema. Nakon što je pregledao loot, baci 33 itema. Koliko je pun inventory na kraju, ako svaki item zauzima jedno mjesto.
Napišite programski.

int maxInventory = 100;
int currentInventory = 67;

currentInventory -= 13; (54)
currentInventory += 45; (99)
currentInventory -= 33; (66)

U inventoryu ima 66 itema


27.) Max HP je 100, body shoot je 20 damage. Vaš lik ima 80 HP na početku borbe i odmah te neprijatelj pogodi "body shoot". Potom padneš i izgubiš 2% HP-a i usput te neki NPC nabije i napraviti damage u iznosu od 8% maxHP-a. Regenerira ti se HP na max, ali te opet neprijatelj pogodi "body shoot" i head shoot koji je duplo veći od body shoota. Jesi li živ na kraju?
Napiši programski.

float HP = 80f;
int maxHP = 100;
int bodyS = 20;
int headS = bodyS * 2;

HP -= bodyS; (60)
HP *= 0.98f;
HP -= 0.08f * maxHP;
HP = maxHP;
HP -= bodyS;
HP -= headS;

28.) Igrač pero ima 4 zlatne lopte, a igrač marko ima 6 zlatnih lopti, igrač pero je marku oteo jednu zlatnu loptu, te mu se poslje marko nasao sa sestrom doma i usput vratio svoju loptu i ukrao dvije perine.
Koliko lopti ima na kraju Marko, koliko Pero i čija sestra je izgubila nevinost?
Napiši programski.

int pero = 4;
int marko = 6;

pero += 1; *NAPOMENA* ili pero++; skraćeni zapis kada povećavate ili smanjujete vrijednost nekoj varijabli za 1
marko --;
pero -= 3;
marko += 3;

Na kraju Marko ima 8, Pero ima 2 lopte, a nevinost je izgubila Perina sestra.

29.) Igrač ima 20 mana. Iskoristio je vatrenu magiju koja vrijedi 5.5 mana, a nakon toga uzastopno dvije ledene magije koje vrijede 3 mane. Koliko mu je mana ostalo?
Napiši programski.

float mana = 20f;
float Vmagija = 5.5f;
int lMagija = 3;

mana -= Vmagija;
mana -= lMagija * 2;

30.) Na prvenstvu se natječu Danska, Hrvatska, Španjolska i Švedska. Svaka pobjeda nosi 2 boda. Danaska je pobjedila Hr, Špan i Šved. Te nakon utakmice sa Šved. zbog pijanstva dobila 2 kaznena boda. Koliko bodova ima danska.

int d = 0;

d += 2 * 3;
d -= 2;

Danska ima 4 boda.

31.) Saša je imao 10 lopti. Dvije je dao psu. Pas je jednu pojeo, koliko je lotpi ostalo psu?

int sasa = 10;
int pas = 0;

sasa -= 2;
pas += 2;
pas--;

Psu je ostala jedna lopta.

32.) Mini ima tank od 60L. Do Novske potroši 20L, cestarina je u jednom smjeru 40 kn. Koliko će novaca izkamatariti tjedno ako putujemo u oba smjera autoputom i cijena goriva je 9 kn/L.

int t = 7; //tjedna dana
int c = 40; (cestarina) 
int p = 20; (potrošnja u jednom smjeru)
int cg = 9; (cijena goriva po litri)
int n = 0; (konačna cijena)

n = t * ((2 * p * cg) + (2 * c));

Novaca je potrošeno 3080 kn tjedno.

33.) Krešo ima 10 jabuka. Ivan mu kriomice pojede 2 jabuke. Koliko je jabuka pojeo Denis, a koliko je ostalo Kreši?

int kreso = 10;
int Ivan = 0;
int denis = 0;

kreso -= 2;
ivan += 2;

34.) Igrač ima 50 HP i 30 mana. Zatim na protivnika baci spell koji troši 3.5 mane. A istovremeno ga pogađa protivnikov spell koji radi 20 dmg. Istovremeno nosis shield koji blokira 50% dmg od tog spella. Koliko HP i mane imaš?

int igracHP = 50;
float igracMana = 30;
float spellM = 3.5f;
int spellDmg = 20;

igracMana -= spellM;
igracHP -= spellDmg * 0.5f;

Igrac ima 40HP i 26.5 mane.

**PISANJE KOMENTARA**
- Obavezno
- koristi se za druge ljude kada čitaju vaš kod
- Korisno ako neko vrijeme niste čitali kod
- Programeri koji ne koriste komentare kažu "Prvo smo Bog i ja znali kako radi ovaj kod, sada zna samo Bog"
- Komentari se pišu sa "//" i pišete ih iznad ili u redu naredbe/linije/metode na koju se odnose
- Komentiranje više redova od jednput

npr. 

int health = 100; // varijabla za Health
int dmg = 25; //varijabla za dmg od enemy zombija
//varijabla za dmg od enemy vampira
int dmg2 = 30;

npr. Komentiranje više redova - koristi se kada kod radi, ali se našao/traži se bolji način pa ga se zakomentira da se ne odvija u programu

/*
float Bana = 89f;

Bana = Bana*2 - 45 + 3.9f;
*/



35. Iskomentirajte i riješite zadatak:
- Max HP je 100, body shoot je 20 damage. Vaš lik ima 89 HP na početku borbe i odmah te neprijatelj pogodi "body shoot". Potom padneš i izgubiš 2% HP-a i usput te neki NPC nabije i napraviti damage u iznosu od 8% maxHP-a. Regenerira ti se HP na max, ali te opet neprijatelj pogodi "body shoot" i head shoot koji je duplo veći od body shoota. Potom je NPC se zbugao i nanio ti istu štetu kao i prije.

float HP = 89f; //trenutni health
int maxHP = 100; //maksimalni health
int bodyS = 20; //Iznos damagea od body shoota
int headS = bodyS * 2; //Iznos damagea od head shota koji je duplo veći od body shoota

HP -= bodyS; //Igrač je primo body shoot i pao je na 69 HP
HP *= 0.98f; //Igrač gubi 2% HP-a
HP -= 0.08f * maxHP; //NPC nanosi štetu igraču u iznosu od 8% maksimalnog healtha što iznos 8 bodova
HP = maxHP; //Igrač se regenenerira u potpunosti i zato mu je health trenutni jednak maksimalnom
HP -= bodyS; //Igrač je primo body shoot i pao je na 80 HP
HP -= headS; //Igrač je primo head shoot i pao je na 40 HP
HP -= 0.08f * maxHP; //NPC nanosi štetu igraču u iznosu od 8% maksimalnog healtha što iznos 8 bodova i igrač ima 32 HP

36.) Pri rezultatu 100/90 domacini su dali jos 3 kosa do kraja utakmice. A gosti su dali 5 koseva. Koliki je bio rezultat na kraju utakmice ako znam da jedan koš vrijedi 2 boda.

int domacini = 100; //broj koseva domacina
int gosti = 90; //broj koseva gostiju
int bodovi = 2; //Broj bodova za jedan kos

domacini += bodovi * 3; //domacinima dodajemo 6 bodova
gosti = bodovi * 5 + gosti; //gostima dodajtemo 10 bodova

Rezultat je bio 106/100

37.) Cwekii ima 20 debila na unity edukacije od kojih mora napraviti game developera, 5 debila odustaje od edukacije, 3 debila se prebacuju sa blendera, ali od pridošlica samo dvojica završavaju. Koliko je game developera Cwekii napravio?

int debili = 20; //početni broj ljudi koji kreću
int odustao = 5; //5 ljudi koji odustanu
int blendZavrsio = 2; //uspijesni broj prebacenih ljudi koji su uspijeli zavrsitit edukaciju
int zavrsili; //varijabla za konacni broj onih koji zavrse

debili -= odustao; //oduzimanje odustalih od ukupnog broja polaznika
debili += blendZavrsio; //dodavanje ljudi koji su se prebacili i završili
zavrsili = debili; //ukupni broj ljudi koji su završili edukaciju

38.) Igrac ima 100 max hp i 50 movement pointsa, također ima 1 potion koji ga heala 10% max HP. Ima dvije vrste napada specijalni koji radi 50 dmg i troši 30 movement pointa i obicni napad koji radi 30 dmg i ne kosta nista. Monster ima 126 HP-a i tuce za 30 dmg. Tuku se naizmjenicno svako jedan napad po krugu, a potion ne troši krug i igrač napada prvi. Combat je gotov kada igrač ili monster umru. Tko umre prvi ako igrač koristi sve što mu je na raspolaganju?

float maxHp = 100f;
float currentHp = maxHP;
int movementPoints = 50;
int potion = 1;
int specialAtc = 50;
int specialAtcCost = 30;
int basicAtc = 30;
int monsterHp = 126;
int monsterAtc = 30;

//Potez Prvi
monsterHp -= specialAtc; //126 - 50 = 76
movementPoints -= specialAtcCost; //50 - 30 = 20
currentHp -= monsterAtc; //100 - 30 = 70

//Potez Drugi
currentHP += potion * 0.1f * maxHp; //70 + 10 = 80
potion--;
monsterHp -= basicAtc; //76 - 30 = 46
currentHp -= monsterAtc; //80 - 30 = 50

//Potez Treći
monsterHp -= basicAtc; //46 - 30 = 16
currentHp -= monsterAtc; //50 - 30 = 20

//Potez Četiri
monsterHp -= basicAtc; //16 - 30 = -14

Prvi umre Monster

**UVIJETI**
Što je uvijet?
- Uvije je skup riječi koje govore što se treba ispuniti kako bi se izvšio niz/red naredbi
- Primjeri uvijeta:
	- AKO - if
	- DOK - while
	- Kada - When (ne postoji, primjera radi samo)
- Kako se zadovoljava uvijet, odnosno provjerava - sa znakovima vrijednosti:

== jednako je (provjera vrijednosti jesu li jednake
= dodjela vrijednosti
!= različito je
|| ovo označava vrijednost ili na tipkovnici se napiše pristiok Alt Gr i W
&& ovo označa vrijednost i
> veće od
< manje od
<= manje ili jednako
>= veće ili jednako

primjeri if petlje:

if( 1+1 == 2) -> točno
if( 2+3 != 4) -> točno
if( 18 > 19) -> netočno
if(21 <= 21) -> točno

kao da piše:
istina -> 1+1=2
laž -> 5+5=11
if(istina || laž) -> istina
if(istina || istina) -> istina
if(laž ||istina) -> istina
if(istina && istina) -> istina
if(istina && laž) -> laž
if(laž && laž) -> laž

39.) x je veličine 8, a y je veličine 13, ako je njihov zbroj manji od 50 neka se x poveća za samoga sebe, a ako je zboj veći ili jednak 50 neka se y poveća za samoga sebe, a u svim ostalim slučajevima x se treba pomnožiti za y i spremiti u x.

int x = 8;
int y = 13;

if(x + y < 50)
{
	x += x;
}

else if(x + y > 50 || x + y == 50) ILI if(x + y >= 50)
{
	y += y;
}

else
{
	x *= y;
}

40.) Vrijednost x je -3.6, a vrijednost y je 10. Ako je njihov umnožak veći od 100 neka se x uveća za taj umnožak, ako je manji od 100 neka se y uveća za taj umnožak, a ako je umnožak jednak 100 neka se x i y uvećaju za sami sebe.


float x = -3.6f;
float y = 10;

if(x * y > 100)
{
	x += x * y;
}

else if(x * y < 100)
{
	y += y * x;
}

else if(x * y == 100)
{
	x += x;
	y += y;
}


*NAPOMENA* 
PRAVILNO IMANOVANJE VARIJABLI:

Pravilno:
_playerOne
playerOne
PlayerOne
player_1
player_One

OK:
player1
playERonE
PLAYERone

Nepravilno:
1player
1_player

41.) Igračev health je 30. Regeneracija ce mu krenuti ako mu je health pao ispod 20 i povećati će se za 12. 

int x = 30;
int y = 12;

if(x < 20)
{
	x += y;
}

42.) Igračev health je 31. Regeneracija ce mu krenuti ako mu je health pao ispod 20 i povećati će se za 12. Igrac pprvo prima dmg od 9. Zatim je popio potion koji mu je vratio 10 hp i potom je primio critical strike koji mu je skinuo 26 hp. Pitanje je jel se igracu dogodio health regen i ako je kada? Napisati programski i oznacite komentarima kada se dogodila regeneracija.

int playerHP = 31;
int boostHP = 12;
int dmg = 9;
int potionHP = 10;
int criticalStike = 26;

if(playerHP < 20) //If uvijet je zadovoljen tek poslje trećeg koraka
{
	playerHP += boostHP;
}

playerHP -= dmg; //Prvi korak - HP = 22
playerHP += potionHP; //Drugi korak - HP = 32
playerHP -= criticalStrike; //Treći korak - HP = 6


43.) Igrac jedan i dva su imali 100 hp. Igrac dva je udario igraca jedan te mu oduzeo 25 hp, igrac jedan kontra napadom oštečuje igrača dva za 14 hp, te regenerira sebe za 10 hp. Koliko koji igrac ima hp?

int playerOne = 100;
int playerTwo = 100;

playerOne -= 25;
PlayerTwo -= 14;
playerOne += 10;

44.) Pokemon evoluira kada dođe na 16 level. Za doći na 16 level potrebno mu je ukupno 4561 XP. Početni level mu je 1 i ima 0 xp. Odradio je 25 borbi gdje je dobio 25 xp po borbi i potom još 25 borbi gdje je u svakoj dobio 93 xp. Napravite provjeru imali dovoljno xp za evoluciju, ako nema gubi sav xp, a ako ima onda mu je level 16.

int currentLevel = 1;
int currentXP = 0;
int fightTwoXP = 93;
int fightOneXP = 25;
int evolveXP = 4561;

currentXP += fightOneXP * 25; //Imamo 625 XP
currentXP += fightTwoXP * 25; //Dobivamo 2325 XP i ukupno imamo 2950 XP

if(currentXP >= evolveXP)
{
	currentLevel = 16;
}

else if(currentXP < evolveXP)
{
	currentXP = 0;
}


45.) Kapetan Miki ima 10 HP, ako primi damage veći od 5, a da ga to neće ubiti onda se Miki naljuti i njegov HP bude 20.

int mikiHP = 10; 
int dmg;
if((dmg > 5) && mikiHP > dmg)
{
	mikiHP = 20;
}

46.) A je jacine 5, a B je jacine 17 ako je njihova razlika veca od 10 neka se broj A poveca za B, a ako je manja ili jednaka neka se B poveca za A

int A = 5;
int B = 17;

if(A - B > 10 || B - A > 10)
{
	A += B;
}

else if(A - B <= 10 || B - A <= 10)
{
	B += A;
}

47.) Imate varijablu X. Ako je X veći od 10, neka se X uveća za 20, a ako je X manji od 10 neka se umanji za 9. U ostalim slučajevima neka se X pomnoži sam sa sobom.

int x;

if(x > 10)
{
	x += 20;
}

else if(x < 10)
{
	x -= 9;
}

else
{
	x *= x;
}

48.) U šumi je 6 elfova, 5 gnomova i 10 orkova, ako je broj elfova i gnomova veći ili jednak orkovima, broj elfova se uveća za njihov broj (poduplaju se), a ako je njihov zbroj manji, orkovi pojedu 50% gnomova i 20% elfova. NAPOMENA, ne može se pojesti decimalno elfova niti gnomova.


int elfs = 6;
int gonms = 5;
int orcs = 10;

if(elfs + gnoms >= orcs)
{
	elfs *= 2;
}

else if(elfs + gnoms < orcs)
{
	gnoms = (int)(gnoms * 0.5f);
	elfs = (int)(elfs * 0.8f);
}


49.) Pocetna vrijednost varijable je 830, X je 69, a Y je 538. ukoliko je umnožak x i y manji od 1000 i varijabla vrijednosti 830 zbrojena sa y je veca od 1000 , neka se varijabla sa pocetnom vrijednoscu 830 smanji za 100. Ukoliko je umnozak veci (samo to je uvije), neka se pocetna varijabla poveca za 100. U svim ostalim slucajevima neka se pocetna varijabla pomnozi za 100.

int x = 69;
int y = 538;
int pocVar = 830;

if(x * y < 1000 && pocVar + y > 1000)
{
	pocVar -= 100;
}

else if(x * y > 1000)
{
	pocVar += 100;
}

else
{
	pocVar *= 100;
}

50.) Super Mario je imao 3 života na početku. izgubio je 2. Dobio je jedan život tokom nivoa i dobio jedan nakon što je prešao nivo. Napiši programski.

int marioLife = 3;

marioLife -= 2;
marioLife++;
marioLife++;

51.) X je 15, Y je 100, ako je x izmedu 13 i 19 (ukljceni su i 13 i 19) povecaj y za 20%. A ako nije onda y smanji za 13%. Ne smiju se spremati vrijednosti u decimalnom zapisu.

int x = 15;
int y = 100;

if( x >= 13 && x <= 19)
{
	y += (int)(0.2f * y);
}

else
{
	y -= (int)(0.13f * y);
}

52.) Cilj igre je skupiti 4 objekta, prsten, pehar, zlatnik i dijamant. Sa svakim skupljenim itemom aktivira se jedan boos, a kada se skupi svih 4 itema, igra je pređena. Odmah skupljate prsten, te dalje tim redosljedom. Sa koliko bossova ste se borili dok niste prešli igru?

int items = 0;
int itemsToCollect = 4;
int prsten = 0;
int pehar = 0;
int zlatnik = 0;
int dijamnt = 0;
int boss = 0;

prsten++;
if(prsten == 1)
{
	boss++;
	items++;
}
pehar++;
if(pehar == 1)
{
	boss++;
	items++;
}
zlatnik++;
if(zlatnik == 1)
{
	boss++;
	items++;
}
dijamant++;
if(dijamant == 1)
{
	boss++;
	items++;
}

if(items == itemsToCollect)
{
	//Kraj igre
}

Ukupno smo se borili sa 4 bossa

53.) Vrijednost "nice" je 69, a vrijednost "blaze" je 420, ako je zbroj tih vrijednosti pomnožen sa 2 veći od 1300 neka se nice podupla, a ako je manji ili jednak 1250 neka se blaze podupla.

int nice = 69;
int blaze = 420;

if((nice + blaze) * 2 > 1300)
{
	nice *= 2;
}

else if((nice + blaze) * 2 <= 1250)
{
	blaze *= 2;
}

54.) Programski zapisi 11.79 + 9.01 = 19.80;

float x = 11.79f;
float y = 9.01f;
float rez = 19.8f; //Koji treba biti 19.80

if (x + y > rez)
{
	float constanta = rez - x - y;
	x += constanta;
}

else if(x + y < rez)
{
	float constanta = rez - x - y;
	x += constanta;
}

55.) igrac HP-a 100 je primio dmg 23. Popijemo potion koji nam vrati 15 hp. Napisi programski.

int HP = 100;
int dmg = 23;
int potion = 15;

HP -= dmg;
HP += potion;

56.) Vi imate 15€, a vaš frend 25€ i vaš treći frend ima 10.5€. Vi takvi siromasi se odlučite kupiti Bugatti Vayrona koji košta 2500000€. Ako osoba koja ima najviše novaca ima duplo više novaca od osobe koja ima najmanje novaca, onda on plaća pola cijene. A ostala dvojica djele drugu polovicu po pola. Ako to nije slučaj onda svi plačaju jednak iznos. Napisite koliko svakome pojedinacno jos fali novaca.

float my = 15f;
float friendOne = 25f;
float friendTwo = 10.5f;
int bugatti = 2500000;

if (my > friendOne && my > friendTwo)
        {
            if (friendOne > friendTwo)
            {
                if(my >= friendTwo * 2)
                {
                    my -= bugatti * 0.5f;
                    friendOne -= bugatti * 0.25f;
                    friendTwo -= bugatti * 0.25f;
                }
                else 
                {
                    my -= bugatti / 3;
                    friendTwo -= bugatti / 3;
                    friendOne -= bugatti / 3;
                }
            }

            else if(friendOne < friendTwo)
            {
                if (my >= friendOne * 2)
                {
                    my -= bugatti * 0.5f;
                    friendOne -= bugatti * 0.25f;
                    friendTwo -= bugatti * 0.25f;
                }
                else
                {
                    my -= bugatti / 3;
                    friendTwo -= bugatti / 3;
                    friendOne -= bugatti / 3;
                }
            }
        }

        if (friendOne > my && friendOne > friendTwo)
        {
            if (my > friendTwo)
            {
                if (friendOne >= friendTwo * 2)
                {
                    friendOne -= bugatti * 0.5f;
                    my -= bugatti * 0.25f;
                    friendTwo -= bugatti * 0.25f;
                }
                else
                {
                    my -= bugatti / 3;
                    friendTwo -= bugatti / 3;
                    friendOne -= bugatti / 3;
                }
            }

            else if (my < friendTwo)
            {
                if (friendOne >= my * 2)
                {
                    friendOne -= bugatti * 0.5f;
                    my -= bugatti * 0.25f;
                    friendTwo -= bugatti * 0.25f;
                }
                else
                {
                    my -= bugatti / 3;
                    friendTwo -= bugatti / 3;
                    friendOne -= bugatti / 3;
                }
            }
        }

        else
        {
            //Frend dva ima najvise ili jednkao kao neki od prijatelja, ali ima vec kredit i hipoteku pa nikom nista
        }


57.) x je 50, y je 100, ako je njihov zboj veci od trostruke vrijednosti x onda neka se x podupla, a ako je manji neka se y udvostruci, a ako su jednaki neka se oba kvadriraju.

int x = 50;
int y = 100;

if(x + y > x * 3)
{
	x *= 2;
}

else if(x + y < x * 3)
{
	y *= 2;
}

else if(x + y == x * 3)
{
	x *= x;
	y *= y;
}

58.) igrac ima 100 HP. Ako mu HP padne ispod 75 regenrira mu se health za 10, a ako padne ispod 25 i igrac ostane ziv, regenerira mu se za 25 HP. Igrac prvo primi dmg od 55, a zatim opet primi dmg od 55. Koliko hp ima igrac?

int HP = 100;

if(HP < 75)
{
	HP += 10;
}

else if(HP < 25 && HP > 0)
{
	HP += 25;
}

HP -= 55; //100 - 55 = 45
//Aktivira se Prva regeneracija i sada ima 45 + 10 = 55 HP
HP -= 55; //55 - 55 = 0
//Nema regeneracije jer je umro

59.) Prvi vozac ima 10 kockica, a drugi ima 13. Kada se oni sudare onaj koji ima manje ukrade drugome jednu kockicu.

int x = 10;
int y = 13;

if(x > y)
{
	y += 1;
	x -= 1;
//skraceno bi bilo y++; i x--;
}

else if(y > x)
{
	y--;
	x++;
}

60.) Varijabla x je 15, varijabla y je dva puta veca od x. Ako je zbroj x i y manji od 50, x se poveca za y. Ako je zbroj x i y veci od 50 od x se oduzima y, a ako je zbroj jednak onda x dobiva y vrijednost.

int x = 15;
int y = 2 * x;

if(x + y < 50)
{
	x += y;
}

else if(x + y > 50)
{
	x -= y;
}
else
{
	x = y;
}

*VAR vrsta varijable

var:
- vrsta varijable koja se prilagođava unosu
- ne može se definirati u početku
- koristi se samo unutar neke petlje/metode za privremenu vrijednost

npr.
Neka se rezultat množenja x i y poveća za 10 i spremite u varijablu rez, ali to odradite u 2 koraka
float x = 10.5f;
int y = 77;
float rez;


var privremeno = x * y;
rez = privremeno + 10;

*Metode*
- metoda je skup naredbi koje se trebaju odraditi u određenim uvijetima i intervalu

Ključne metode u Unityu:

- Awake - odvija se prije 0 (nultog) framea
	- događa se prije stvaranja bilokakvih objekata na sceni
	- izvršava se samo jedanput

- Start - izvršava se netom prije prvog framea
	- izvršava se samo jedanput

- Update - Svaki frame se jednom izvrši

-kako se piše metoda:

void nazivMetode()
{
	linija naredbe;
}

* vidljivost metoda/varijabli
- public - ispred vrste varijable kojoj omugucavamo da mi kao developeri možemo unositi i mjenjati vrijednost varijable u Unity Inspectoru ili pozivati iz drugih skripti
npr. public float primjer = 4.7f;

- private - stavlje se ispred varijable i onemogucava vidjljivost i promjene vrijednosti varijable iz Unity Inspectora i pozivanje iz drugi skripti. Ako ispred varijable/metode ne piše ništa, automatski je private.
npr. private int primeric = 5; ili int primeric = 5;

*Debugiranje*
- postupak pružanja informacija developeru ima li greške i koja je greška ili ispis rada programa u konzoli

- Osnovo - Debug.Log("")
- Debug -> debugiraj (provjeri)
- Log -> ispiši u konzoli
- ("") - unutar navodnika tekst za ispis, poslje može varijabla za ispis (spajanje se radi sa pomoću znaka +)

npr. 1.
public int x;
public int y;

void Start()
{
	var rez = x * y;
	Debug.Log("Rezultat je" + rez);
}

npr. 2.

void Start()
{
	Debug.Log("Odvila se start metoda");
}

Napomena za zadatke 61.- ako ne pise da je javna varijabla onda je privatna

61.) U startu neka se zbroje 2 javna broja. I neka se ispiše u konzoli njihov zbroj.

public int x = 2;
public int y = 4;

void Start()
{
	var rez = x + y;
	Debug.Log(rez);
}

62.) Zbrojite u startu x koji je 17.2 i y koji je javan broj. Neka se rezultat sprema u varijablu "banana" i neka se ispiše u konzoli.

float x = 17.2f;
public int y;
float banana;

void Start()
{
	banana = x + y;
	Debug.Log(banana);
}

63.) Povecajte x koji je 5 za 5 svaki frame.

int x = 5;

void Update()
{
	x += 5;
}

64.) Neka je x 6, a y je 7 i njihov zbroj neka se izracuna u startu, a onda svaki frame neka se rezultat uveca za x

int x = 6;
int y = 7;
int rezultat;

void Start()
{
	rezultat = x + y;
}

void Update()
{
	rezultat += x;
}

65.) Neka je x 8, a y je javni broj i njihov zbroj neka se izracuna u startu, a onda svaki frame neka se rezultat poveca za polovicu x varijable.

int x = 8;
public int y;
int xy;

void Start()
{
	xy = x + y;
}

void Update()
{
	xy += x * 0.5f;
}

66.) Ako je vrijednost x -3.6, a vrijednost y je javna. Ako je zbroj veci od 20 neka se ispiše "To legendo", a ako je manji ili jednak neka se ispiše "Falia si budalo"

float x = -3.6f;
public flaot y;

void Start()
{
	if(x + y > 20)
	{
		Debug.Log("To legendo");
	}
	
	else if(x + y <= 20)
	{
		Debug.Log("Falia si budalo");
	}
}

67.) U startu zbrojite dvije javne varijable od kojih se jedna zove "y". Neka se svaki frame x povecava za 5% iznosa y varijable. Svaki frame ponovo zbrojite x i y te ispišite vrijednost zbroja.

public int y;
public float x;
float rezultatxy;

void Start()
{
	rezultatxy = x + y;	
}

void Update()
{
	x += 0.05 * y;
	rezultatxy = x + y;
	Debug.Log(rezultatxy);
}

68.) Neka se u metodi start ispiše "Ja sam Start", u metodi awake "Ja sam mali Awake), te u konacnici neka se svaki frame ispisuje "Moje ime je medvjedić Lino".

69.) Noice

70.) Povecajte javnu varijablu X za jedan svaki frame. Neka se također svaki frame ispiše "Ja imam " + x + " godina".

Ponavljanje - DZ:

1.) napišite primjer int varijable
int a = 69;

2.) napišite primjer float varijable
float y = 6.996f;

3.) Objasnite razliku između || (OR/ILI) i && (AND/I) sa primejrom if-else u start metodi

int x = 2;
int z = 1;
int y = 3;

void Start()
{
	if(x + y < 6 && z + x < 6)
	{
		x++;	
	}
	else if(x + y == 5 || z + z + x < 4)
	{
		z += 2;
	}
}

4.) Što znači !=

Označava da je vrijednost različita

5.) x je 10, a y je 12. U Startu napravite provjeru ako su x i y jednaki neka se ispiše "Jednako je", ako su različiti neka se ispiše "Različito je"

int x = 10;
int y = 12;

void Start()
{
	if(x == y)
	{
		Debug.Log("Jednako je");
	}
	else if(x != y)
	{
		Debug.Log("Različito je");
	}
}

6.) Ivica ima 5 jabuka, a Marica ima 2 jabuke, Ivica joj uzme 1 jabuku. Marica izjavi da je Ivica Smrad. Napišite programski

int i = 5;
int m = 2;

void Start()
{
	i++;
	m--;
	Debug.Log("Ivica smrade!");
}

7.) X je javni broj. Neka se prije nultog framea ispiše vrijednost x.

public int x;

void Awake()
{
	Debug.Log(x);
}

8.) U startu zbroji javnu vrijednost x i javnu vrijednost y. Ako je njihov zbroj 10, neka se svaki frame ispiše "Može", a ako njihov zborj nije 10 neka se svaki frame ispiše "Reka sam nemože!"

public int x;
public int y;

void Update()
{
	if(x + y == 10)
	{
		Debug.Log("Može");
	}		
	else if(x + y != 10)
	{
		Debug.Log("Raka sam nemože!");
	}
}

Nastavak:
71.) Imajte dva javna cijela broja. Njihov rezultat zbrajanja se sprema u javnu varijablu. Postupak je sljedeći. Svaki frame se prvi cijeli broj mora povecati za 1, potom se racuna rezultat zbrajanja prvog broja i drugog. Ako je rezultat manji od 500 ispisuje se ("Manji"), ako je između 500 i 2000 ispisuje se "Prosječan", a ako je veći ispisuje se "Največi"

public int x;
public int y;
public int rez;

private void Update()
{
	x++;
	rez = x + y;
	if(rez < 500)
	{
		Debug.Log("Manji sam!");
	}
	else if(rez > 500 && rez < 2000)
	{
		Debug.Log("Prosječan sam!");
	}
	else
	{
		Debug.Log("Največi sam hehe ;)!");
	}
}

72.) x ima vrijednost 7, ali je x privatna varijabla. Napišite da se u startu ispiše vrijednost x varijable.

int x = 7;

void Start()
{
	Debug.Log(x);
}

73.) U početku zbroji javnu vrijednost koja prima vrijednosti cijelih brojeva i javnu vrijednost koja prima vrijednost decimalnih brojeva. Ako je njihov zbroj 10 neka se svaki frame ispiše "Banana", a ako je manji od 10 neka se ispiše "Mrkvica". Obavezno koristiti samo jednu metodu.

public int x;
public float y;

void Update()
{
	if(x + y == 10)
	{
		Debug.Log("Banana");
	}
	else if(x + y < 10)
	{
		Debug.Log("Mrkvica");
	}
}

74.) Napišite skriptu gdje se zbrajaju dva javna broja u metodi start i ispisuje im se rezultat na primjer: Rezultat zbrajanja brojeva 8 i 9.5 je 17.5

public int x;
public float y;
public float zbr;

void Start()
{
	zbr = x + y;
	Debug.Log("Rezultat zbrajanja brojeva " + x + " i " + y + " je " + zbr);
}

*Bool
- Što je to Bool?
- Bool je vrsta varijable koja pohranjuje u sebe isključivo 2 moguće vrijednosti, a to su točno ili netočno.
- Može biti samo True ili False
- Tehnički se može reći da ide 0 ili 1
npr.
bool banana = false;
bool died = true;
bool gameEnded; //Kada ne piše vrijednost onda je null, ako je null to je kao da je 0, a pošto ne može biti 0 onda je false
bool zmajcek = false; <=> bool zmajcek;

75.) Neka se zbrajaju dva javna broja, ako je njihov zbroj veci od 15 neka bool endGame postane true, ako su manji ili jednaki neka je false. Ovo napravite u drugoj, vlastitoj metodi, Ako je endGame true onda neka se ispiše u konzoli da je igra završena, a ako je false neka ispiše da si preživio.

public int x;
public int y;
bool endGame = false; //bool endGame;

void Start()
{
	if(x + y > 15)
	{
		endGame = true;
	}
	else
	{
		endGame = false;
	}
	CheckIfEndGame();
}

void CheckIfEndGame()
{
	if(endGame == true)
	{
		Debug.Log("Igra je završila");
	}
	else if(endGame == false)
	{
		Debug.Log("Preživio/la si");
	}
}

76.)Neka su dva javna broja float vrijednosti. Za pocetne vrijednosti neka budu 12.3 i 18.2. Definirajte i privatni bool banana. Banana je na pocetku false. Ako je umnožak dva broja veci ili jednak 100 neka banana postane true i u konzoli neka se ispiše "Banana je pojedena", ako je banana false neka se ispiše "Banana je citava". 
Riješite na dva načina. 1. koristeći samo metodu Awake. 2. koristeći metodu start i vlastitu metodu.

1. način

public float x = 12.3f;
public float y = 18.2f;
private bool banana = false; //bool banana;

void Awake()
{
	if(x * y >= 100)
	{
		banana = true;
		Debug.Log("Banana je pojedna");
	}
	else if(banana == false)
	{
		Debug.Log("Banana je citava);
	}
}

2. Način

public float x = 12.3f;
public float y = 18.2f;
private bool banana = false; //bool banana;

void Start()
{
	if(x * y >= 100)
	{
		banana = true;
	}
	else
	{
		banana = false;
	}
	BananaPojedena();
}

void BananaPojedena()
{
	if(banana == true)
	{
		Debug.Log("Banana je pojedna");
	}
	else
	{
		Debug.Log("Banana je cijela");
	}
}

77.) Napišite skriptu gdje ćete imati start metodu i metodu naziva vašeg imena. U startu neka se pozove metoda vašeg imena. Metoda vašeg imena neka ima naredbu da ispiše u konzoli "Ja sam unity developer i pametniji sam nego gobelodt developer".

void Start()
{
	Marko();
}

void Marko()
{
	Debug.Log("Ja sam unity developer i pametniji sam nego gobelodt developer");
}

78.) Napišite skriptu koja u sebi nosi 4 javna floata. U startu se radi provjera matematickog zapisa:
x + y * (a / b) - x / a * 18 * b - x + y = rez
Ako je rezultat veci od drugog zapisa:
x - y / (a * b) + x * a / 18 / b + x - y + 23 = rez
Onda neka se u updateu rezultat povecava za jedan i ispisuje "Ja sam rezultat: " + rezultat

public float x;
public float y;
public float a;
public float b;

float rez1;
flaot rez2;
bool jelOk = false;

// public float x, y, a, b;

void Start()
{
	rez1 = x + y * (a / b) - x / a * 18 * b - x + y;
	rez2 = x - y / (a * b) + x * a / 18 / b + x - y + 23;

	if(rez1 > rez2)
	{
		jelOk = true;
	}
}

void Update()
{
//kada imate bool u if-u i samo njega imate onda možete napisati "if(jelOk)" to je provjera jel taj bool true
	if(jelOk == true) 
	{
		rez1++;
		Debug.Log("Ja sam rezultat: " + rez1);
	}
}

79.) Napišite skriptu gdje imate jedan bool i jedan javni broj. Ako je taj javni broj razlicit od 18 onda neka se pozove metoda MjenjajMe koja mjenja vrijednost tako da ako je bool bio true pretvara ga u false, a ko je bio false pretvara ga u true.

public int x;
bool jabuka;

void Start()
{
	if(x != 18)
	{
		MjenjajMe();	
	}
}

void MjenjajMe()
{
	if(jabuka == true)
	{
		jabuka = false; // !jabuka
	}
	else if(jabuka == false)
	{
		jabuka = true;
	}
}

80.) Napišite skriptu koja množi dva javna broja. Ako je njihov umnožak različit od 100 ili je veci od 0 onda neka se pozove druga metoda naziva JaSamMetodaDva u kojoj ce se raditi djeljenje prvog broja sa drugim i ako je rezultat djeljenja manji od 100 neka se ispiše "Ja sam MVP" (Inače MVP je kratica za Most Valuable player), ako je bilokoje drugo riješenje neka se u konzoli ispiše "Ja sam N00b".

public int x;
public int y;

void Start()
{
	if(x * y != 100 || x * y > 0)
	{
		JaSamMetodaDva();
	}
}

void JaSamMetodaDva()
{
	if(x / y < 100)
	{
		Debug.Log("Ja sam MVP");
	}
	else
	{
		Debug.Log("Ja sam N00b");
	}
}


81.) Imate javni bool. Ako je taj bool jednak true u startu onda neka se ispiše "Volim mrkvu", a ako je različit od true onda neka se ispiše "Volim borovnicu".

public bool bolic;

void Start()
{
	if(bolic == true) //bolic
	{
		Debug.Log("Volim mrkvu");
	}
	else if(!bolic) // else if(bolic == false) // else if(bolic != true)
	{
		Debug.Log("Volim borovnicu");
	}
}

82.) Sto ce se ispisati?

public int x = 10;
public int y = 3;

void Start()
{
	Debug.Log(x/y);
}

Ispisati će: 3 jer su obje vrijednosti u int-u a ne pohranje se vrijednost u float

83.) Uvecaj int x za 1 svaki drugi frame.

int x = 0;
int brojac = 1;

void Update()
{
	if(brojac % 2 == 0)
	{
		x++;
	}
	brojac++;
}

84.) x i y su javni brojevi. Ako je njihov zbroj manji od 5 u konzoli ispiši "Mužu, prevarila sam te", a ako je veći ispiši "Dijete je tvoje!"

public int x;
public int y;
int zbroj;

void Start()
{
	zbroj = x + y;
	
	if(zbroj < 5)
	{
		Debug.Log("Mužu, prevarila sam te");
	}
	
	else if(zbroj > 5)
	{
		Debug.Log("Dijete je tvoje!");
	}
}


*Nova petlja - For petlja

for => dok je uvijet zadovoljen obavlja naredbe
int i => brojac, ne mora se zvati i, najcešce se zove i
uvijet => uvijet koji mora biti zadovoljen
inkrement => utjece na brojac, naredba s kojim djelujemo na brojac

nacin pisanja for petlje:

for(int i = 0; uvijet; inkrement)
{
	naredba;
}

primjer:

int broj_enemya = 3;

void Start()
{
	for(int i = 0; i < broj_enemya; i++)
	{
		Debug.Log("Stvoren je " + i + ". enemy!");
	}
}

85. Zadatak za for petlju
Napravite skriptu koja ima *maximalno* 3 if/else if petlji po metodi. Neka je svaki uvijet definiran bool-om.
Prvi bool se zove "easy" drugi se zove "normal", a treci "harderDaddy".
a) ako je svaki bool false na pocetku, neka bool "normal" postane true
b) svaki bool mora biti javan
c) za uvijet easy neka se broj enemy-a (varijabla enemies) stavi na 5 i neka joj je to pocetna vrijednost (varijable)
d) Ako je igra na normal onda neka je broj enemya dva puta veci od easy nacina
e) Ako je igra na harderDaddy onda neka je broj enemya tri puta veci nego u normal nacinu igre
f) Nakon što se provjeri koji je mod igre (težina, odnosno koji je bool tocan), neka se ispiše u konzoli svaki enemy posebno kao u primjeru: Debug.Log("Stvoren je " + i + ". enemy!") - npr. Stvoren je 3. enemy | Stvoren je 10. enemy
g) Kada su se ispisali svi enemy-i (prestala je for petlja iz zadataka 
f) neka se ispiše poruka "Sretno ti u " + težina igre + " nacinu igre!" - npr. Sretno ti u easy nacinu igre!

Prvi Nacin:
----------------------------------------------------------------------------

public bool easy;
public bool normal;
public bool harderDaddy;
int enemies = 5;

void Start()
{
	if(easy == false && normal == false && harderDaddy == false)
	{
		normal = true;
	}
	Spawn();
}

void Spawn()
{
	if(easy)
	{
		enemies = 5;
		for(int i = 1; i <= enemies; i++)
		{
			Debug.Log("Stvoren je " + i + ". enemy!")
		}
		Debug.Log("Sretno ti u easy nacinu igre");
	}

	else if(normal)
	{
		enemies *= 2;
		for(int i = 1; i <= enemies; i++)
		{
			Debug.Log("Stvoren je " + i + ". enemy!")
		}
		Debug.Log("Sretno ti u normal nacinu igre");
	}
	else if(harderDaddy)
	{
		enemies *= 6;
		for(int i = 1; i <= enemies; i++)
		{
			Debug.Log("Stvoren je " + i + ". enemy!")
		}
		Debug.Log("Sretno ti u harderDaddy nacinu igre");
	}
}

------------------------------------------------------------
Drugi nacin:
------------------------------------------------------------

public bool easy = false;
public bool normal = false;
public bool harderDaddy = false;
int enemies = 5;

void Start()
{
	if(easy == true)
	{
		enemies = 5;
		Spawn();
		Debug.Log("Sretno ti u easy nacinu igre");
	}
	else if(harderDaddy == true)
	{
		enemies *= 2 * 3;
		Spawn();
		Debug.Log("Sretno ti u harderDaddy nacinu igre");
	}
	else
	{
		normal = true;
		enemies *= 2;
		Spawn();
		Debug.Log("Sretno ti u normal nacinu igre");
	}
}

void Spawn()
{
	for(int i = 1; i <= enemies; i++)
	{
		Debug.Log("Stvoren je " + i + ". enemy!")
	}
}


*Ponavljanje*
86.) Kakav zapis ide u vrstu varijable int?

87.) Kakav zapis ide u vrstu varijable float?

88.) Kakav zapis ide u vrstu varijable bool?

89.) Ako je int ostavljen prazan, vrijednost mu je?

90.) Ako je bool ostavljen prazan, vrijednost mu je?

91.) Napišite programski matematicke zapise:
5 * (22 - 11) / 8 = x
9 * 7 * x / z = y
x * 88 - 111 = z

92.) Koja je razlika izmedu 3 standardne Unity metode sa primjerom funkcionalnosti iz pravih video igara: 
a) Awake
b) Start
c) Update

93.) Prepišite skriptu i prepravite greške:

int x = 11:
float nm = 2.4;
flaot z = 0f;

void Start
(
	z == nM * 2.5 - X;
)

94.) x je javan broj koji je 100. Ispisi u konzoli sve parne brojeve od 0 do x.

95.) Napisi primjer for petlje.

96.) Prepišite skriptu i prepravite greške te joj napišite komentare

flaoat X_X = 67f;
publik float xD:
private float c_N = 99.54321f;

void Update()
{
	if(X_X * xD - cn >= 69.22 * 2;
	[
		X_X -- 19;
		xD *= 1.2f;
		c_M -= 1,1f;
	]


97.) Prepišite skriptu i prepravite greške te joj napišite komentare

boll marmun = false;
intx = 11:
int z = 12;
int y:

void Start[]
{
	if(x / 11 & z - 12 = 1)
	{
		marmun = true;
		y = z * X;
}

Void Update()
{
	y = y * Z * x:
}

** NOVA VRSTA VARIJABLE **

-string 

string je tekstualni zapis, mogu se zapisivati rijeci, slova, brojevi i znakovi

npr.

string ime = "Pero";
string prezime = "Peric";
string datumRodenja = "19.1.2011";
string visina = "189 cm";


98) Napišite skriptu koja ucitava upisane javne vrijednosti za igracevo ime, prezime, broj godina (int), visinu (int), te grad iz kojega dolazi. U metodi start se trebaju ispisati u konzoli sve njegove vrijednosti.
npr. "Ja sam Marko Maric. Imam 28 godina, te sam visok 169 cm i dolazim iz mjesta koje se zove Špickovina!"


public string ime;
public string prezime;
public string grad;
public int godine;
public int visina;


void Start()
{
	Debug.Log("Ja sam " + ime + " " + prezime + ". Imam " + godine + " godina, te sam visok " + visina + " cm i dolazim iz mjesta koje se zove " + grad + "!");
}

99.) Napišite skriptu koja ucitava javne vrijednosti, zatim te podatke ispisuje u navedeni tekst:
"Moje ime je _________ i ja dolazim iz grada koji se zove ___________, a višlji sam od covjeka koji se zove __________ za ______ cm". To znaci a morate imati u kodu ucitavanje 2 vrijednosti imena, 2 vrijednosti visine i 2 vrijednosti grada, te dvije solucije teksta ovisno o tome koji je lik višlji.

public string ime1;
public string ime2;
public int visina1;
public int visina2;
public string grad1;
public string grad2;

void Start()
{
	if(visina1 > visina2)
	{
		Debug.Log("Moje ime je " + ime1 + " i ja dolazim iz grada koji se zove " + grad1 + ", a višlji sam od čovjeka koji se zove " + ime2 + " za " + (visina1 - visina2) + " cm");
	}
	
	else if(visina1 < visina2)
	{
		Debug.Log("Moje ime je " + ime2 + " i ja dolazim iz grada koji se zove " + grad2 + ", a višlji sam od čovjeka koji se zove " + ime1 + " za " + (visina2 - visina1) + " cm");
	}
}

*NOVO*


WHILE PETLJA (dok petlja)

odvija s dok je tvrdnja tocna
OPASNA ZA UNITY! - ako je napisano krivo ruši (crasha) Unity, ali ne samo Unity nego u gorim situacijama može izbrisati dio projekta i/ili ugasiti racunalo

Kako se piše while petlja:

while(uvijet)
{
	naredba;
	naredba koja utjece na uvijet;
}

*Infinity loop* - while petlja (ne samo while) koja se ponavlja u beskonacnost jer joj uvijet nikada ne može prestati vrijediti jer naredba koja utjece na nju ne radi pravilno

Primjer pravilnog:

int broj = 4;

void Start()
{
	while(broj > 0)
	{
		Debug.Log("Broj je: " + broj);
		broj--;
	}
}

Primjer infinity loopa:

int broj = 4;

void Start()
{
	while(broj > 0)
	{
		Debug.Log("Broj je: " + broj);
		broj++;
	}
}

KAKO NE KORISTI WHILE PETLJU!!!

bool gameOver;
bool spawned;
float timer;

void Update()
{
	while(gameOver == false)
	{
		if(spawned == false)
		{
			//ucini nešto
		}
		else if(timer >= 2.0f)
		{
			timer += Time.deltaTime;
		}
	}
}



100.) Napišite while petlju koja je istinita dok god je int x vece ili jednako od 0. U while petlji treba biti naredba koja x smanjuje za 2, te ispisuje u konzoli "Ja volim " + x.

public int x;

void Start()
{
	while(x >= 0)
	{
		Debug.Log("Ja volim " + x);
		x -= 2;
	}
}

101.) Napišite skriptu koja ce ugasiti igru ako je slucajno igracev health pao ispod 0 ili je 0.
Naredba koja gasi igru (.exe) se piše: Application.Quit();

public float health = 100f;

void Update()
{
	if(health <= 0)
	{
		Application.Quit();
	}
}


102.) Napišite skriptu koja ce imati vašu vlasititu metodu. Ta vaša vlastita metoda mora ispisati u konzoli "Prošlo je 60", svaki put kada se prikaže 60 frameova. (brojac koji broji frameove se resetira nakon 60 frameova i ponovo broji do 60 i tako dok god skripta radi)

int frameCounter = 0; //boj koliko se frameova pokazalo

void Update()
{
	frameCounter++;
	FrameCounterShow();
}

void FrameCounterShow()
{
	if(frameCounter == 60)
	{
		Debug.Log("Prošlo je 60");
		frameCounter = 0;
	}
}

103.) Napišite for petlju koja piše u konzoli odbrojavanje (npr. 10, 9, 8, 7...), ali krece od pocetne javne vrijednosti, te ne smije pasti ispod 0 (zadnji broj zapisani u ispisu treba biti 0). U slucaju da je broj upisan negativan (npr. -102) onda ga se treba pretvoriti prvo u pozitivan i krenuti odbrojavati, a ako je 0 onda se treba pretvoriti u 10.

public int pocetno;

void Start()
{
	if(pocetno < 0)
	{
		pocetno *= -1;
	}
	else if(pocetno == 0)
	{
		pocetno = 10;
	}

	for(int i = pocetno; i >= 0; i--)
	{
		if(i > 0)
		{
			Debug.Log(i + ",");
		}
		else if(i == 0)
		{
			Debug.Log(i + ".");
		}
	}
}

104.) Vrijednost X je javna, dok god je vrijednost y = 10 (pocetna y je 10 iako je y javan), oduzmite od X y (ne pohranjujete rezultat u X), te ako je rezultat oduzimanja negativan broj (manji od 0) neka se ispiše "Negativo", ako je veci ispišite "Pozitivno", a ako je 0 neka se ispiše "Neutralno". Svaki put kada se ispiše tekst u konzoli neka se y umanji za 0.5.

105.) Napišite skriptu koja unosi igraceve informacije (username, ime championa, level, hp + 2 po izboru). Podatci se trebaju ispisati "Player (username) is playing with (champion). (username) is (level) level starting with: (hp) + (2 vase varijable)" Podatci se ispisuju tako da svaki frame se hp smanjuje za 1. Kada je igracu HP pao ispod 0 ili je 0 neka se ispiše SAMO JEDNOM "Player (username) has been killed". Poslje ispisa da je igrac umro se ne smije više ništa ispisivati.

106.) Imamo 2 public int-a nazovimo ih start i end. 
U Start methodi ispisati -1 ako je start broj manji od nule ili end broj manji od nule ili end manji od start broja.
Ako nije onda zbrojiti sve neparne brojeve izmedu start (inkluzivno) i end (inkluzivno) brojeva. 
Nakon toga ispisati taj zbroj. Inkluzivno znaci ukljucujuci.

107.) Imamo public int nazovimo ga number. U Start methodi ispiši -1 ako je public int (number) negativan.
A ako nije negativan ispisi zbroj prve i zadnje znamenke.
Tip: Koristi while.

* Primjeri: *
-> ako je number 252 -> ispisati se treba 4 jer prva znamenka je 2 (gledano s ljeva) i zadnja je (gledano s ljeva) pa 2 + 2 = 4
-> ako je number 257 -> ispisati se treba 9 jer 2 + 7 = 9 -> 2 je prva 7 je zadnja
-> ako je number 0 -> ispisati se treba 0
-> ako je number 10 -> ispisati se treba 1 jer 1 + 0 = 1
-> ako je number 12587 -> ispisati se treba 8 jer je 1 + 7 = 8
-> ako je number 5 -> ispisati se treba 10, jer je 5 prva i zadnja znaci 5 + 5 = 10
-> ako je number -10 -> ispisati se treba -1 jer je -10 negativan

108.) Definiraj public broj koji representira/predstavlja broj kilobajta.
Ako je broj kilobajta negativan u Start methodi ispiši text "Broj ne valja.".
A ako je broj kilobajta positivan ispiši koliko je to megabajta i kilobajta.

* Primjeri: *
Ako su kilobajti 1025 ispisati "1025 KB je 1 MB i 1 KB."
Ako su kilobajti -10 ispisati "Broj ne valja."
Ako su kilobajti 3000 ispisati "3000 KB je 2 MB i 952 KB."

Tip: 1 MB (MegaBajt) je 1024 KB (kiloBajta)

109.) Što ce uciniti ova metoda Update?

int x= 4;

void Update()
{
	while(x>0)
	{	
		if(x>0)
		{ 
			Application.Quit();
		}
		x++;
	}	
}

110.) Neka je X 4, a Y 5 i neka se njihov zbroj izracuna u startu u varijablu Z, a onda neka se svaki frame Z uveca za X.

111.) Napisi metodu Odbrojac koja odbrojava od 5 do 0 i koristi while petlju. Metoda ispisuje brojeve od 5 do 0.

112.) Napiši for petlju koja prvi frame ispisuje neparne brojeve od x do y koji je 90.

113.) Crashaj Unity! Ozbiljno, napiši skriptu za koju misliš da će 100% crashati unity. Slobodno koristi internet.

114.) Ispišite sve brojeve od javne vrijednosti do nula (javna vrijesnot se može unjeti samo pozitivan broj)

115.) Ispišite sve parne vrijednosti od neke javne vrijednosti do 0

116.) Neka se zbrajaju dva javna broja, ako je njihov zbroj veci od 15 neka bool endGame postane true, ako su manji ili jednaki neka je false. Ako je endGame true onda neka se ispiše u konzoli da je igra završena, a ako je false neka ispiše preživio si.

*TEŠKI ZADATCI ZA KRAJ:

117.) Isprogramirajte osnovni krizic kruzic. (bez programskog djela unosa X i O igraca).
Dakle imajte dve javne vrijednosti za imena igraca. Imajte dodjelu koji je igrac X a koji O. Dodjelite vrijednosti svih 9 polja. Napravite svoju skriptu koja provjerava ako je jedan igrac napravio niz od 3 za pobjedu.

118.) Napišite skriptu gdje igrac unosi 15 ocijena (Hrv, Mat, Eng itd.). U slucaju da ima 3 ili više jedinica neka mu se ispiše da pada razred, u suprotnom neka mu se izracuna prosjek.

119.) Napravite program za upis u srednju školu. U inspectoru se unose vrijednosti hrv za 7 i 8 razred i isto tako za eng i matematiku. Zatim neka su dva javna stringa za dva predmeta koji se traze (npr. fizika i kemija) sa ocjenama iz 7 i 8 razreda. Zatim se gleda prosjek ocijena iz 5,6,7 i 8 razreda. Broj bodova je zbroj ocijena svih unesenih. Imajte provjeru da unesena ocijena mora biti veca od jedan i manja od 6.

120.) Napišite skriptu koja ce nam imati sva obilježja championa iz MOBA tipa igre. Takoder pripremite i moci, dmg za njega sve potreben informacije koje smatrate da jedan MOBA champion ima. (Nemojte zaboraviti provjeru jel umro, respawn itd.) i sve iskomentirati.

121.) Napišite skritpu gdje igrac unosi rijecima boju: (CRVENA, BIJELA, PLAVA, CRNA, ŽUTA, ZELENA) te mu se u konzoli za tu boju ispisuje HEX-a vrijednosti i RGB vrijednost kao npr.
Unos = BIJELA
U konzoli:
R: 255, G: 255, B: 255
#FFFFFF
Vrijednosti mozete naci na internetu: 
https://www.rapidtables.com/web/color/RGB_Color.html

122.) Neka se upisuje vrijednost u intu, a zatim string sa igracevim imenom. Potom neka se za vrijednost inta ispise ime npr. ako je ime "Marko" a int 3 onda ce se ispisati:
"MarkoMarkoMarko" i pohrati ce se vrijednost u string imena.

123.) Rafaelina papiga, cije je ime Papigapapigapapigapapigapapiga, cim nauci neku novu rijec, pocinje je
izgovarati uzastopno bez prestanka. Rafaela je uocila da ce papiga svaku rijec najprije izgovoriti tocno pet
puta bez stanke. Na primjer, rijec „deda“ bit ce izgovorena kao „dedadedadedadedadeda“.
Napiši program koji, za zadanu rijec, ispisuje papigin izgovor te rijeci.
Npr. Deda - DedaDedaDedaDedaDeda, ho - hohohohoho
